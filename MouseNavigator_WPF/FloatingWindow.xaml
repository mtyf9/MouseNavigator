<Window x:Class="MouseNavigator_WPF.FloatingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MouseNavigator_WPF"
        mc:Ignorable="d"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize"
        Width="300" Height="300">
    <Grid>
        <Ellipse Width="300" Height="300" Fill="#77FFFFFF"/>
        <Button x:Name="Button12"
        HorizontalAlignment="Center" VerticalAlignment="Top"
        Margin="0,5,0,0" Content="Top Button"
        Style="{DynamicResource FanShapeButtonStyle}"/>
        
        <Button x:Name="Button6"
        HorizontalAlignment="Center" VerticalAlignment="Bottom"
        Margin="0,5,0,0" Content="Bottom Button"
        Style="{DynamicResource FanShapeButtonStyle}" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="180"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>
    </Grid>
    
    <Window.Resources>
        <Style x:Key="FanShapeButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Path Fill="{TemplateBinding Background}" Stroke="Black"
                          Data="M 0,50 A 50,50 0 0 1 100,50 L 0,50 Z" />
                            <!-- 扇叶形状 -->
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkGray" />
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

</Window>
